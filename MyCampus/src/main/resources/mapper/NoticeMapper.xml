<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wang.mycampus.mapper.NoticeMapper">

    <!-- 前台：启用公告，最新在前 -->
    <select id="selectActive" resultType="com.wang.mycampus.vo.NoticeVO">
        SELECT id, title, content, status,
               DATE_FORMAT(create_time, '%Y-%m-%d %H:%i:%s') AS createTime,
               DATE_FORMAT(update_time, '%Y-%m-%d %H:%i:%s') AS updateTime
        FROM notice
        WHERE status = 1
        ORDER BY create_time DESC
    </select>

    <!-- 后台：分页条件查询 -->
    <select id="selectPage" resultType="com.wang.mycampus.vo.NoticeVO">
        SELECT id, title, content, status,
               DATE_FORMAT(create_time, '%Y-%m-%d %H:%i:%s') AS createTime,
               DATE_FORMAT(update_time, '%Y-%m-%d %H:%i:%s') AS updateTime
        FROM notice
        <where>
            <if test="keyword != null and keyword != ''">
                title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectById" resultType="com.wang.mycampus.pojo.Notice">
        SELECT id, title, content, status, create_time, update_time
        FROM notice WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notice(title, content, status, create_time, update_time)
        VALUES(#{title}, #{content}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <!-- 更新：只更新非空字段（停用/启用仅传 status 时不清空其它字段），
         并强制 update_time=NOW() 刷新版本号（数据库 ON UPDATE 只在行数据变化时触发） -->
    <update id="update">
        UPDATE notice
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM notice WHERE id = #{id}
    </delete>

</mapper>
